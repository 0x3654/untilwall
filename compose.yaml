services:
  untilwall:
    container_name: untilwall
    image: docker.io/0x3654/untilwall:latest
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./.data:/app/data
    restart: always
